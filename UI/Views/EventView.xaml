<UserControl x:Class="CalendarApp.Views.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CalendarApp.Views" xmlns:viewmodels="clr-namespace:CalendarApp.ViewModels"
			 mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Button Background="Aquamarine" HorizontalAlignment="Stretch" VerticalAlignment="Top" Command="{Binding EventClickedCommand}" ToolTip="{Binding Title}">
		<TextBlock Foreground="Black" Text="{Binding Title }" />
	</Button>
</UserControl>