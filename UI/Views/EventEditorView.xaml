<Window x:Class="CalendarApp.Views.EventEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalendarApp.Views"
		xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="EventEditorView" Height="300" Width="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"></ColumnDefinition>
			<ColumnDefinition Width="247*"></ColumnDefinition>
		</Grid.ColumnDefinitions>
		<TextBlock TextWrapping="Wrap" Text="Title" Grid.Row="0" Grid.Column="0" Margin="5" />
		<TextBlock TextWrapping="Wrap" Text="Description" Grid.Row="1" Grid.Column="0" Margin="5" />
		<TextBlock TextWrapping="Wrap" Text="Start Time" Grid.Row="2" Grid.Column="0" Margin="5" />
		<TextBlock TextWrapping="Wrap" Text="End Time" Grid.Row="3" Grid.Column="0" Margin="5" />

		<TextBox TextWrapping="Wrap" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0, 0, 5, 20" BorderBrush="Black" />
		<TextBox TextWrapping="Wrap" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0, 0, 5, 20" BorderBrush="Black" />
		<xctk:DateTimePicker Grid.Row="2" Value="{Binding StartTime}" Grid.Column="1" Margin="0, 0, 5, 20"></xctk:DateTimePicker>
		<xctk:DateTimePicker Grid.Row="3" Value="{Binding EndTime}" Grid.Column="1" Margin="0, 0, 5, 20"></xctk:DateTimePicker>

		<StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button Content="Save" IsEnabled="{Binding AnyUnsavedData}" Command="{Binding SaveCommand}" Margin="0, 0, 5, 0" />
			<Button Content="Cancel" Command="{Binding CancelCommand}" />
		</StackPanel>
	</Grid>
</Window>